# Aplicação da M5.2

Extraia este pacote na raiz do projeto e execute:

```bash
uv run ruff check src tests
uv run pytest
```

Commit sugerido:

```bash
git add .
git commit -m "feat(execution): add execution intelligence and preflight checks"
```
